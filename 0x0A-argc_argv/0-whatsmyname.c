#include <stdio.h>

/**
* main - write a program that prints its name,follwed by a nw line.
* @argc: This is the argument count
* @argv: This is the argument vector
*
* Return: This returns to 0.
*/

int main(int arg, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
